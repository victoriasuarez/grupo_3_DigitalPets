<!DOCTYPE html>
<html lang="en">
    <%- include('../partials/head', {styles: ['normalize', 'styles','header','footer','account']}) %>
<body>
    <%- include ('../partials/header') %>

    <main class="main-login">
        <h1>Bienvenido</h1>
        <form class="form-login" method="post" action="/users/login">
            <label for="email">E-mail</label>
            <input type="email" id="email" name="email">
            <% if(locals.errors?.email) { %>
                <p class="error-message"><%= errors.email.msg %></p> 
            <% } %>
            
            <label for="password">Contraseña</label>
            <input type="password" id="password" name="password">
            <% if(locals.errors?.password) { %>
                <p class="error-message"><%= errors.password.msg %></p> 
            <% } %>

            <label>
                <input type="checkbox" name="remember"> Recordarme
            </label>
            
            <input type="submit" value="Iniciar Sesión">
        </form>
        <div class="p">
            <p><a href="#">¿Olvidaste tu contraseña?</a></p>
            <p>¿No tienes cuenta? <a href="/users/register">Regístrate</a></p>
        </div>
    </main>
    <%- include ('../partials/footer') %>
</body>
</html>